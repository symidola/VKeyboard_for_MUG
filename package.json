{
  "name": "vkeyboard-for-mug",
  "private": true,
  "workspaces": [
    "client",
    "injector",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev -w server\" \"npm:dev -w client\"",
    "build": "npm -w shared run build && npm -w client run build && npm -w server run build && npm -w injector run build",
    "start": "npm -w server run start",
    "typecheck": "npm -w shared run build && npm -w client run typecheck && npm -w server run typecheck && npm -w injector run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
